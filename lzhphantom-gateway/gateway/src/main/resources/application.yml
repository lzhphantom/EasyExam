spring:
  profiles:
    active: dev
  application:
    name: lzhphantom-gateway
  config:
    import: optional:nacos:lzhphantom-gateway-dev.yml
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id: true
      routes:
        - id: lzhphantom-user
          uri: lb://lzhphantom-user
#          #                uri: http://localhost:10010
          predicates:
#            # Path Route Predicate Factory ????? /lzhphantom/user/** ??????????? http://localhost:10010 ??uri?
            - Path=/lzhphantom/user/**
#              # Weight Route Predicate Factory ???????????????????????80%
#            # ???group1?????????????
            - Weight=group1, 8
#          filters:
#            - StripPrefix=2 # StripPrefix????????????1?????/lzhphantom
        - id: exam
          uri: lb://exam
          predicates:
            - Path=/lzhphantom/exam/**
          #filters:
            #- StripPrefix=2 # LzhphantomRequestGlobalFilter代码中统一处理
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
      username: nacos
      password: nacos
server:
  port: 80
